<%= form_for(@loteactividad) do |f| %>
  <% if @loteactividad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loteactividad.errors.count, "error") %> prohibited this loteactividad from being saved:</h2>

      <ul>
      <% @loteactividad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="small-3 columns">
      <%= f.label :campo_id %>
      <%= f.collection_select(:campo_id, Campo.all.where(:empresa_id => @current_empresa.id),:id, :nombre, {} ,{:id=>'Campo'}) %>
    </div>
    <div class="small-3 columns">
      <%= f.label :lote_id %>
      <%= f.collection_select(:lote_id, Lote.where(:campo_id => Campo.where(:empresa_id => @current_empresa.id).first), :id, :lote,{},{:id=>'Lote'}) %>
    </div>
    <div class="small-3 columns">
      <%= f.label :actividad_id, "Actividad" %>
      <%= f.collection_select :actividad_id, Actividad.all.where(:empresa_id => @current_empresa.id), :id, :actividad %>
    </div>
    <div class="small-3 columns">
    </div>
  </div>

  <div class="row">
    <div class="small-3 columns">
       <%= f.label :superficie %>
      <%= f.text_field :superficie %>
    </div>
    <div class="small-3 columns">
       <%= f.label :produccion %>
      <%= f.text_field :produccion %>
    </div>
    <div class="small-3 columns">
       <%= f.label :precioproduccion, "Precio" %>
      <%= f.text_field :precioproduccion %>
    </div>
    <div class="small-3 columns">
    </div>
  </div>

  <div class="row">
    <div class="small-12 columns">
      <ul class="button-group">
        <li><button class="tiny" type="submit">Aceptar</button></li>
      </ul>
    </div>
  </div>
  
<% end %>
